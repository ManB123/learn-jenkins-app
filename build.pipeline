pipeline {
    agent any
    parameters{
        string(name: 'Name', defaultValue:'Me', description:'whats your name')
    }
    environment{
        String YourName = "${params.Name}"
    }
    stages {
        stage('Hello') {
            steps {
                echo "Hello ${YourName}"
            }
        }
    }
}
